import moxios from 'moxios'

describe('createCoupon', () => {
  beforeEach(() => {
    moxios.install()
  })

  afterEach(() => {
    moxios.uninstall()
  })

  test('', () => {})
})

// METHOD USING MOXIOS TO TEST ANY ACTION CALLS
